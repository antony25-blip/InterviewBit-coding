class Solution:
    # @param A : tuple of integers
    # @return an integer
    def repeatedNumber(self, A):
        #count=[None]*len(A)
        #for i in range(len(A)):
        #    a=A[i]
        #    if count[a]!=1:
        #        count[a]=1
        #    else:
        #        return a
        sat=set()
        for i  in range(len(A)):
            if A[i] not in sat:
                sat.add(A[i])
            
            else:
                return A[i]
    
    
